{
  "name": "gaanapy-cloudflare-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for the GaanaPy Render backend",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
